# apps/mobile Ownership

- Purpose: Mobile app shell and feature UI implementation.
- Must not import from `apps/web`.
- Must consume shared packages for domain, tokens, primitives, runtime, and Supabase clients.
